
&НаСервере
Процедура УтвердитьНаСервере()
	Объект.Статус = Перечисления.СтатусыЭталонов.Принят;
	Объект.ПринялРешение = Пользователи.ТекущийПользователь();
	Объект.Записать();
КонецПроцедуры

&НаКлиенте
Процедура Утвердить(Команда)
	УтвердитьНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
КонецПроцедуры
