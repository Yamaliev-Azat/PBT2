
&НаСервере
Процедура ЗаполнитьНаСервере()

	ПредопреденныеДанныеНаСервере.ЗаполнитьНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура Заполнить(Команда)
	
	ЗаполнитьНаСервере();
	
	Элементы.Список.Обновить();
	
КонецПроцедуры
